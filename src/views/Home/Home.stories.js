import React from "react";
import { Home } from "./Home";

export default {
  title: "RickAndMorty/Views/Home",
  component: Home,
  argTypes: {},
};

const Template = (args) => <Home />;

export const HomeView = Template.bind({});
